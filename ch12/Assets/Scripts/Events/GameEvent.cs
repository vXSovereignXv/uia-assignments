using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameEvent
{
    public const string HEALTH_UPDATED = nameof(HEALTH_UPDATED);
    public const string LEVEL_COMPLETE = nameof(LEVEL_COMPLETE);
    public const string LEVEL_FAILED = nameof(LEVEL_FAILED);
    public const string GAME_COMPLETE = nameof(GAME_COMPLETE);
}
